{
  "name": "la-verdad-herald",
  "version": "1.0.0",
  "description": "News publishing platform built with Laravel + React",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "start": "cd backend && php artisan serve --host=0.0.0.0 --port=$PORT",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "cd backend && php artisan serve",
    "build": "npm run build --workspace=frontend",
    "test": "npm run test --workspace=frontend && cd backend && php artisan test",
    "install:all": "npm install && cd backend && composer install",
    "setup": "npm run install:all && npm run setup:db",
    "setup:db": "cd backend && php artisan migrate:fresh --seed"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}