{
  "name": "la-verdad-herald-backend",
  "private": true,
  "scripts": {
    "start": "php artisan serve --host=0.0.0.0 --port=$PORT",
    "dev": "vite",
    "build": "vite build"
  },
  "devDependencies": {
    "axios": "^1.7.2",
    "laravel-vite-plugin": "^1.0.4",
    "vite": "^5.3.1"
  }
}
