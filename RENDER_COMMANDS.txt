# Copy and paste these commands ONE BY ONE in Render Shell

# Step 1: Run migrations
php artisan migrate --force

# Step 2: Seed categories
php artisan db:seed --class=CategorySeeder --force

# Step 3: Clear caches
php artisan config:clear && php artisan cache:clear

# Step 4: Verify categories (should show 12)
php artisan tinker --execute="echo \App\Models\Category::count();"

# If categories still 0, run this:
php artisan tinker --execute="\$cats=['News','Sports','Opinion','Literary','Features','Specials','Art']; foreach(\$cats as \$n){\App\Models\Category::firstOrCreate(['name'=>\$n],['slug'=>strtolower(\$n)]);} echo 'Done';"
